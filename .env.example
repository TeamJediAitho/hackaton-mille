# OpenAI obbligatorio (embedding text-embedding-3-small + LLM di generazione).
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
# Alias accettato da baseline_naive_rag.py se OPENAI_MODEL non è impostato:
# DATAPIZZA_MODEL=gpt-4o-mini

QDRANT_PATH=outputs/qdrant
COLLECTION_NAME=caso_dei_mille
MAX_CONTEXTS=5

# Costo autodichiarato per domanda (EUR).
# DECLARED_COST_EUR=0.0
COST_INPUT_PER_MILLION_EUR=0.15
COST_OUTPUT_PER_MILLION_EUR=0.60
COST_CACHED_INPUT_PER_MILLION_EUR=0.075
